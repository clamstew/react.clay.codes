// adds the default values to the
// package.json
//
//
const defaultPackageValues = {
  'publicFolder': '/public',
  'markdownFolderInPublicFolder': '/blog-markdown',
  'postFolder': '/blog-pages',
  'blogConfig': '/blog-config/config.json'
};

//
// takes user through each confg
// assume user is in creat react app and add instructions on how to
// unless they change directories
// and put custom directions for what to do next